---

#  /opt/softwareag/MWS/bin/mws.sh -s default init as owcs_devops

  - name: Include tasks
    include_tasks: "do_run_mws_util.yml"
    vars:
      _mws_command_params:
        - "-s"
        - "{{ mws_instance_name }}"
        - "init"