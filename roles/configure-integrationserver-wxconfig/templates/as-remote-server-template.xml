<?xml version="1.0" encoding="UTF-8"?>
<config>
    <autoConfig>
    {% if rvar_wxconfig_remoteservers_list is defined and rvar_wxconfig_remoteservers_list | length > 0 %}
        <remoteServerList>
        {% for wxconfig_remoteserver in rvar_wxconfig_remoteservers_list %}
            <remoteServer alias="{{ wxconfig_remoteserver.alias | default() | bool | lower }}" updateIfExists="{{ wxconfig_remoteserver.updateIfExists | default() | bool | lower }}" destroyOnShutdown="{{ wxconfig_remoteserver.destroyOnShutdown | default() | bool | lower }}">
            </remoteServer>
        {% endfor %}
        </remoteServerList>
    {% endif %}
    </autoConfig>
</config>
